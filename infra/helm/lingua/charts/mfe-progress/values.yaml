image:
  repository: lingua/mfe-progress

port: 8080
probePath: /healthz

ingress:
  enabled: true
  subdomain: mfe-progress

resources:
  requests:
    cpu: 10m
    memory: 32Mi
  limits:
    memory: 128Mi

# nginx-unprivileged runs as uid 101.
runAsUser: 101

{
  "name": "svc-ai-dialog",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/svc-ai-dialog/src",
  "projectType": "application",
  "tags": ["scope:ai-dialog", "type:service"],
  "targets": {
    "serve": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/svc-ai-dialog",
        "command": "node --env-file=../../.env --watch -r @swc-node/register src/main.ts"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{projectRoot}/dist"],
      "options": {
        "cwd": "apps/svc-ai-dialog",
        "command": "tsc -p tsconfig.app.json"
      }
    },
    "prisma-generate": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/svc-ai-dialog",
        "command": "prisma generate"
      }
    },
    "prisma-migrate": {
      "executor": "nx:run-commands",
      "options": {
        "cwd": "apps/svc-ai-dialog",
        "command": "prisma migrate dev"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/svc-ai-dialog/jest.config.ts"
      }
    }
  }
}
